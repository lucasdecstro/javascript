const name = "Lucas"; // String

const num = 5; // Number
const numTwo = 5.5; // Number

let studentName; // undefined
let lastNameStudent = null; // null -> explicit

const aproved = true; // Boolean -> true or false (logical)

console.log(typeof name, studentName);
console.log(lastNameStudent);

const a = [1, 2];
const b = a;

console.log(a, b);

b.push(3);
console.log(a, b);

// Symbol * ? *
